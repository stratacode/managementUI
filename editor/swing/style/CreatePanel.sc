CreatePanel {
   override innerType;

   override row2y; 

   override nameFieldRatio;

   override addLayerMode; 
   override createLayerMode; 
   override layerMode; 

   override objEnabled; 
   override  layerEnabled; 
}
